{
  "name": "hireic-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "hireIC root — schemas and tests live here; mcp-server is its own package",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:schema": "vitest run tests/schema",
    "test:mcp": "vitest run mcp-server/src"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@types/node": "^25.9.0",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "gray-matter": "^4.0.3",
    "pinyin-pro": "^3.28.1",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "yaml": "^2.9.0"
  }
}
