{
  "name": "hireic-mcp",
  "version": "0.1.0",
  "description": "MCP server for hireIC. Exposes list_jobs and list_candidates from any hireIC-compatible GitHub repo.",
  "type": "module",
  "bin": {
    "hireic-mcp": "./dist/local.js",
    "hireic": "./bin/hireic.mjs"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "dev": "tsx src/local.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.0"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "gray-matter": "^4.0.3"
  }
}
